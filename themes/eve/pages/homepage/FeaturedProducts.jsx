import React from "react";

// Este componente não renderiza nada na tela.
export default function FeaturedProducts() {
  return null;
}

export const layout = {
  areaId: "content",
  sortOrder: 15,
};

// (Opcional) Se houver uma query exportada, pode manter ou remover, pois não será usada.
export const query = null;
